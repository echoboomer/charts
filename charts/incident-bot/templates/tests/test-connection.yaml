apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "incident-bot.fullname" . }}-test-connection"
  labels:
    {{- include "incident-bot.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "incident-bot.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
